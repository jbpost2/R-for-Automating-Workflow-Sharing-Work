library(ggplot2)

shinyUI(fluidPage(
  
  # Application title
  titlePanel(" "),
  
  # Sidebar with options for the data set
  sidebarLayout(
    sidebarPanel(
      
    ),
    
    # Outputs  
    mainPanel(
     
      )
  )
))
