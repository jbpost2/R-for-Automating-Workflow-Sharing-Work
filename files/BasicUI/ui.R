library(ggplot2)

ui<-fluidPage(
  
  # Application title
  titlePanel(" "),
  
  # Sidebar with options
  sidebarLayout(
    sidebarPanel(
    ),
    
    # Show output
    mainPanel(
    )
  )
)
